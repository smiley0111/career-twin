fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
python-dotenv>=1.0.1
openai>=1.54.0
instructor>=1.6.4

# 我自己的 SDK: 统一 LLM 客户端 + 成本追踪
# 本地开发建议: pip install -e ../../dodu-ai-toolkit (可实时同步 SDK 修改)
dodu-ai @ git+https://github.com/smiley0111/dodu-ai-toolkit.git@main

# Stage 4 RAG: 向量检索
chromadb>=0.5.0           # 向量库, 本地文件持久化

# 本地 embedder (EMBEDDING_PROVIDER=local 时才需要). 默认走 API 模式, 不装它:
# 它会拖进 onnxruntime (~200MB, 且在部分 Windows/容器上有 DLL 加载问题).
# 想本地跑 fastembed 的话: pip install "fastembed>=0.4.0"
# fastembed>=0.4.0
