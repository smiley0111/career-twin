# 后端 API 地址. next.config.js 会把 /api/* 反向代理到这里.
# 本地开发: 指向本机 uvicorn
BACKEND_URL=http://127.0.0.1:8001

# 部署到 Vercel 时, 在 Vercel 项目的 Environment Variables 里设成后端公网 URL, 例如:
# BACKEND_URL=https://career-twin-backend.up.railway.app
